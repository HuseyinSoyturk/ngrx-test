name: Zart Zurt 

on:
  pull_request:
    branches: [master]

jobs:
  test_zart_zurt_zirt:
    runs-on: ubuntu-latest
    steps:
      -uses: actions/checkout@v2
      -uses: actions/setup-node@v1
      -run: npm ci
      -run: npm lint
      -run: npm run build
